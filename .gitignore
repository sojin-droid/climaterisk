# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
frontend/climaterisk/dist/

# Runtime data (sessions, jobs, run results) — not committed
data/sessions/
data/*.db
data/*.db-wal
data/*.db-shm
data/runs/
# Local hazard catalog: large HDF5 binaries (machine-generated, >100MB) — rebuild
# from real sources via scripts/build_hazard.py; never committed.
data/hazard_db/
# Open-data files fetched via the Data tab (Natural Earth, WorldPop, …) — re-fetchable.
data/downloads/
# Persisted impact-function calibration records (worker/climaterisk_worker/vulnerability.py);
# derived from login-gated loss data, re-created by a calibration run.
data/calibrations/

# Project-local CLIMADA conda env (multi-GB, machine-specific) — rebuild from
# worker/climaterisk_worker/env_climada.yml, never committed.
.climada-env/
# Conda / CLIMADA cache is large and machine-specific
worker/.climada_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# Env
.env
